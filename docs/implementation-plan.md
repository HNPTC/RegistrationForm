# 樣板化表單系統實作計畫

## 專案目標

建立一個完整的樣板化表單系統,讓非技術人員能透過**圖形化介面**輕鬆管理表單配置,支援:
- 🎨 視覺化編輯表單欄位
- 🔄 欄位開關與重複使用
- 📱 LIFF ID 和 LINE OA ID 樣板化管理
- 🌐 GitHub Pages 部署支援
- 💾 本地與線上編輯彈性

---

## 系統架構

### 檔案結構

```
RegistrationForm/
├── index.html                    # 原有表單 (不動)
├── template-system/              # 新增:樣板系統目錄
│   ├── config-editor.html        # 圖形化配置編輯器
│   ├── form-template.html        # 動態表單樣板頁面
│   ├── form-renderer.js          # 表單渲染引擎
│   ├── editor-app.js             # 配置編輯器邏輯
│   ├── styles.css                # 共用樣式
│   └── configs/                  # 配置檔目錄
│       ├── default-config.json   # 預設配置範本
│       ├── event-2026-01.json    # 活動1配置
│       └── field-library.json    # 可重複使用的欄位庫
├── docs/                         # 使用文檔
│   ├── implementation-plan.md    # 本檔案
│   ├── user-guide.md             # 使用者指南
│   └── workflow.md               # 工作流程說明
└── README.md                     # 專案說明
```

---

## 核心功能設計

### 1. 圖形化配置編輯器 (`config-editor.html`)

#### 功能特性:
- ✅ **拖曳式欄位管理** - 可拖曳調整欄位順序
- ✅ **即時預覽** - 左側編輯,右側即時顯示表單效果
- ✅ **欄位開關** - 切換按鈕控制欄位啟用/停用
- ✅ **欄位庫** - 從預設欄位庫快速新增常用欄位
- ✅ **匯入/匯出** - 下載 JSON 或上傳現有配置
- ✅ **表單設定** - 編輯 LIFF ID、LINE OA ID、GAS URL

#### 介面布局:
```
┌─────────────────────────────────────────────────┐
│  樣板表單配置編輯器                              │
├──────────────────┬──────────────────────────────┤
│  左側:編輯區      │  右側:即時預覽                │
│                  │                              │
│  [基本設定]       │  ┌────────────────────┐     │
│  表單標題: ___    │  │  📝 活動報名表      │     │
│  LIFF ID: ___    │  │  [圖片]            │     │
│  LINE OA: ___    │  │  ▢ 姓名            │     │
│                  │  │  ○ 性別            │     │
│  [欄位管理]       │  │  ○ 年齡區段        │     │
│  ☰ 姓名 [編輯]   │  │  ☑ 從哪得知        │     │
│  ☰ 性別 [編輯]   │  │  [送出報名]        │     │
│  ☰ 年齡 [編輯]   │  └────────────────────┘     │
│                  │                              │
│  [+ 新增欄位]    │                              │
│  [匯出JSON]      │                              │
└──────────────────┴──────────────────────────────┘
```

---

### 2. JSON 配置檔格式

#### 完整配置範例:

```json
{
  "formMeta": {
    "title": "頭頸專研活動報名表",
    "version": "1.0",
    "liffId": "2008806287-9a1hHluF",
    "lineOaId": "@246trduk",
    "gasUrl": "https://script.google.com/macros/s/AKfycbx74tnCe5wk-9WXWR-6Nrb8VE1R3iaXq33T6tpitgjk0PCYreNSoilkQ6QSDhghf4jlPQ/exec"
  },
  
  "banner": {
    "enabled": true,
    "imageUrl": "https://i.duk.tw/xIs9FV.png",
    "altText": "講座橫幅"
  },
  
  "infoBlocks": [
    {
      "id": "activity-info",
      "enabled": true,
      "title": "活動內容",
      "content": "十六年經驗專業物理治療師授課,現場分析肩頸僵硬來源、學習肩頸狀況自我檢測及舒緩方法。\n\n入場當天還可參加抽獎,有機會獲得小禮物哦！\n\n免費入場。座位有限,額滿為止。"
    },
    {
      "id": "location",
      "enabled": true,
      "title": "地點",
      "placeName": "頭頸專研物理治療所",
      "address": "台北市信義虎林街164巷34號1樓",
      "showMap": true,
      "mapQuery": "頭頸專研物理治療所+台北市信義虎林街164巷34號1樓"
    }
  ],
  
  "formFields": [
    {
      "id": "session",
      "type": "radio",
      "enabled": true,
      "required": true,
      "title": "報名場次",
      "options": [
        "1/14 (三) 10:00 - 11:30",
        "1/19 (一) 10:00 - 11:30",
        "1/25 (日) 10:00 - 11:30",
        "1/30 (五) 10:00 - 11:30"
      ]
    },
    {
      "id": "realName",
      "type": "text",
      "enabled": true,
      "required": true,
      "title": "姓名",
      "placeholder": "請輸入您的真實姓名"
    },
    {
      "id": "gender",
      "type": "radio",
      "enabled": true,
      "required": true,
      "title": "性別",
      "layout": "horizontal",
      "options": ["男", "女"]
    },
    {
      "id": "age",
      "type": "radio",
      "enabled": true,
      "required": true,
      "title": "年齡區段",
      "options": ["20歲以下", "21-35歲", "36-50歲", "51-65歲", "65歲以上"]
    },
    {
      "id": "source",
      "type": "checkbox",
      "enabled": true,
      "required": false,
      "title": "從哪得知 (可複選)",
      "options": ["網路", "親友推薦", "實體傳單或海報", "治療所現場得知"]
    },
    {
      "id": "remind",
      "type": "remind-section",
      "enabled": true,
      "required": true,
      "title": "是否接收活動提醒",
      "methods": {
        "line": { "enabled": true },
        "email": { "enabled": true }
      }
    }
  ]
}
```

---

### 3. 欄位庫 (`field-library.json`)

預先定義常用欄位,方便重複使用:

```json
{
  "library": {
    "basicInfo": {
      "name": {
        "id": "realName",
        "type": "text",
        "title": "姓名",
        "placeholder": "請輸入您的真實姓名",
        "required": true
      },
      "gender": {
        "id": "gender",
        "type": "radio",
        "title": "性別",
        "layout": "horizontal",
        "options": ["男", "女"],
        "required": true
      },
      "phone": {
        "id": "phone",
        "type": "text",
        "title": "聯絡電話",
        "placeholder": "請輸入手機號碼",
        "required": true
      },
      "email": {
        "id": "email",
        "type": "email",
        "title": "Email",
        "placeholder": "example@email.com",
        "required": false
      }
    },
    "demographics": {
      "ageGroups": {
        "id": "age",
        "type": "radio",
        "title": "年齡區段",
        "options": ["20歲以下", "21-35歲", "36-50歲", "51-65歲", "65歲以上"],
        "required": true
      }
    },
    "marketing": {
      "source": {
        "id": "source",
        "type": "checkbox",
        "title": "從哪得知 (可複選)",
        "options": ["網路", "親友推薦", "實體傳單或海報", "治療所現場得知"],
        "required": false
      }
    }
  }
}
```

---

## GitHub 部署方案

### 純前端方案 (推薦)

#### 運作方式:
1. **本地編輯** - 使用 `config-editor.html` 編輯配置
2. **下載 JSON** - 點擊「匯出 JSON」下載配置檔
3. **上傳到 GitHub** - 將 JSON 檔案放入 `template-system/configs/` 目錄
4. **推送更新** - `git push` 到 GitHub
5. **線上使用** - 訪問 `https://yourusername.github.io/RegistrationForm/template-system/form-template.html?config=event-2026-01`

#### 優點:
✅ 完全靜態,GitHub Pages 原生支援  
✅ 不需要後端服務  
✅ 配置檔案版本控制  

---

## 提議的變更

### 新增檔案清單

1. **config-editor.html** - 圖形化配置編輯器主頁面
2. **form-template.html** - 動態表單樣板頁面
3. **form-renderer.js** - 表單渲染引擎核心邏輯
4. **editor-app.js** - 配置編輯器應用邏輯
5. **styles.css** - 共用樣式表
6. **default-config.json** - 預設配置範本
7. **field-library.json** - 可重複使用的欄位庫

---

## 技術細節

### URL 參數載入配置
```javascript
// form-template.html 中的載入邏輯
const urlParams = new URLSearchParams(window.location.search);
const configName = urlParams.get('config') || 'default-config';
const configUrl = `./configs/${configName}.json`;

fetch(configUrl)
  .then(res => res.json())
  .then(config => renderForm(config));
```

### 即時預覽同步
```javascript
// editor-app.js 中的同步邏輯
function updatePreview() {
  const config = getCurrentConfig();
  const previewFrame = document.getElementById('preview');
  previewFrame.contentWindow.postMessage({
    type: 'UPDATE_CONFIG',
    config: config
  }, '*');
}
```

---

## 未來擴展可能

- 🎨 **主題系統** - 支援多種視覺主題
- 📊 **資料統計** - 整合 Google Sheets 資料視覺化
- 🔔 **提醒排程** - 自動化提醒發送管理
- 🌍 **多語系** - 支援英文、日文等語言
- 📱 **行動優化** - 編輯器支援平板操作
